digraph G {
    // duas fontes
    A -> C;
    A -> B;

    // ramos paralelos
    C -> E;
    E -> H;

    B -> D;
    D -> F;
    F -> B;

    // reconvergências (cruzamentos entre os ramos)
    E -> D;   // reconvergência 1
    H -> F;   // reconvergência 2
}
